package dataStructure;

class Solution {

    public String solution(String S, String C) {
        String answer = "";
        String fn = "";
        String mn = "";
        String ln = "";
        String[] arr = S.split(",");
        String[] subArr = {};
        String subArrStr = "";
        for(int i = 0; i < arr.length; i++){
            subArr = arr[i].split(" ");
            if(subArr.length == 2){
                fn = subArr[0].charAt(0);
                answer+=)
            }
        }
            System.out.println(answer);
            return answer;
    }
}
